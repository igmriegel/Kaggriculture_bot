# Submission

A submission contains `main.py` at the package root and only runtime assets.
Validate from a directory outside the repository, without internet, against
`random` and self-play. Reports, development dependencies, and local paths must
not enter the package.
